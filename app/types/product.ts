export type Product = {
  id: string;
  name: string;
  price: number;
  brand: string;
  image: string;
  description: string;
  category: string;
  sizes: number[];
  colors: string[];
  sellerId: string;
};